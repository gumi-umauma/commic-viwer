## プロジェクト概要

漫画管理システム（学習用）

- **目的**: AI活用を前提とした設計手法を実践で学ぶ
- **本番想定**: 学術大会の参加登録・演題申込システム
- **学ぶ手法**: SDD(Spec-Driven Development)、DDD、クリーンアーキテクチャ、t-wadaのTDD

## 技術スタック

- **Framework**: Next.js (App Router) - フルスタック構成
- **Database**: PostgreSQL
- **DB Access**: sqlc（ORMなし、SQLから型安全なコード生成）

## 仕様駆動開発（OpenSpec）

本プロジェクトでは **OpenSpec** を使った仕様駆動開発を採用。コードを書く前に「何を作るか」を自然言語で明確化し、AIと合意してから実装を進める。

### ディレクトリ構成

```
openspec/
├── specs/      # 現在のシステム仕様（source of truth）
└── changes/    # 進行中の変更（変更ごとにフォルダ管理）
```

### ワークフロー（スラッシュコマンド）

| コマンド | 説明 |
|---------|------|
| `/opsx:new <名前>` | 新しい変更を開始 |
| `/opsx:continue` | 次の成果物（proposal/specs/design/tasks）を作成 |
| `/opsx:ff` | 全成果物を一括作成 |
| `/opsx:apply` | タスクを実装 |
| `/opsx:archive` | 完了した仕様をspecs/にマージ |

### 変更フォルダの構成

```
openspec/changes/<変更名>/
├── proposal.md   # 意図とスコープ
├── specs/        # 差分仕様（ADDED/MODIFIED/REMOVED）
├── design.md     # 技術的アプローチ
└── tasks.md      # 実装チェックリスト
```

## 重要なルール

1. **最小限の作業**: ユーザーの要求に対して必要最小限の作業のみ行う。勝手に機能を追加・拡張しない
2. **提案は確認してから**: 実施が望ましい事項は、実行前に必ずユーザーに確認・提案する
3. **教えながら作る**: ユーザーは各手法の経験がないので、解説しながら進める
4. **AIが主導して質問する**: 仕様の曖昧な点・不足点はAIが洗い出してユーザーに質問する。ユーザー任せにしない
